{
  "name": "ateam-contracting-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for ateamcontractings.com. No build step — this file exists so Netlify installs dependencies for netlify/functions.",
  "type": "module",
  "scripts": {
    "test": "node --test netlify/functions/_lib/routes.test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@netlify/blobs": "^9.1.6"
  }
}
